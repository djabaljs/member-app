{% extends 'base.html.twig' %}

{% block title %}Direction index{% endblock %}

{% block body %}
    <h1>Direction index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for direction in directions %}
            <tr>
                <td>{{ direction.id }}</td>
                <td>{{ direction.name }}</td>
                <td>
                    <a href="{{ path('direction_show', {'id': direction.id}) }}">show</a>
                    <a href="{{ path('direction_edit', {'id': direction.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('direction_new') }}">Create new</a>
{% endblock %}
